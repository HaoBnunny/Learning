Before parsing, there comes 'lexxing' which just means to convert the code you have written into 'tokens' which the parser can read.
Parser converts these tokens into an abstract syntax tree.
In laymen terms, it just means that it makes an order instruction set