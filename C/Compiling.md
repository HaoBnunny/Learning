 The process of converting the '.i' file into a source ('.s') file that contains assembly-level translations of your code.

You can generate a .s by running 
```Shell
gcc -S example.c -o example.s
clang -S example.c -o example.s
```
